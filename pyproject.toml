[build-system]
requires = ["setuptools>=64", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "franka-control-client"
version = "0.1.0"
authors = [
    {name = "Jakub Suliga", email = "j.p.suliga@gmail.com"},
    {name = "Xinkai"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pytest>=8.3.5",
    "pyzmq>=26.4.0",
]

[tool.setuptools]
packages = ["franka_control_client"]
package-dir = {"" = "src"}
